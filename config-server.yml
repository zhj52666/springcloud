spring:
 profiles:
  active: dev

---
server: 
 port: 8201
spring:
 profiles:
  active: dev
   application:
    name: springcloud-provider-dept
eureka:
  client:
    service-url:  # 修改默认端口
      defaultZone: http://www.eureka1.com:7001/eureka/
  instance:
    instance-id: springcloud-config-client-3355 # 修改eureka 默认描述信息
    prefer-ip-address: true  # ip 信息
# info 配置
info:
  app.name: zhj-springcloud
  company.name: www.zhjweb.tech
---
server:
 port: 8202
spring:
 profiles: test
  application:
   name: springcloud-provider-dept
eureka:
  client:
    service-url:  # 修改默认端口
      defaultZone: http://www.eureka1.com:7001/eureka/
  instance:
    instance-id: springcloud-config-client-3355 # 修改eureka 默认描述信息
    prefer-ip-address: true  # ip 信息
# info 配置
info:
  app.name: zhj-springcloud
  company.name: www.zhjweb.tech